# Key Bindings
# key name refer to `xev` or `wev` command output,
# mod keys name: super,ctrl,alt,shift,none

# System
bind=SUPER,r,reload_config
bind=SUPER+CTRL,semicolon,spawn,sh -c "systemctl poweroff ; loginctl poweroff"
bind=SUPER+CTRL+ALT,semicolon,spawn,sh -c "systemctl reboot ; loginctl reboot"

# Spawn applications
bind=SUPER,Return,spawn,kitty
bind=SUPER,b,spawn,helium
bind=SUPER+CTRL,space,spawn,sh -c 'bemenu-run -l 10 -p Run'

# exit
bind=SUPER,m,quit
bind=SUPER,q,killclient

# Argos dwm config
bind=SUPER,p,zoom

# switch window focus
bind=SUPER,Tab,focusstack,next
bind=ALT,Left,focusdir,left
bind=ALT,Right,focusdir,right
bind=ALT,Up,focusdir,up
bind=ALT,Down,focusdir,down

# swap window
bind=SUPER+SHIFT,Up,exchange_client,up
bind=SUPER+SHIFT,Down,exchange_client,down
bind=SUPER+SHIFT,Left,exchange_client,left
bind=SUPER+SHIFT,Right,exchange_client,right

# switch window status
bind=SUPER,g,toggleglobal,
bind=ALT,Tab,toggleoverview,
bind=ALT,backslash,togglefloating,
bind=SUPER,a,togglemaximizescreen,
bind=SUPER,f,togglefullscreen,
bind=ALT+SHIFT,f,togglefakefullscreen,
bind=SUPER,i,minimized,
bind=SUPER,o,toggleoverlay,
bind=SUPER+SHIFT,I,restore_minimized
bind=SUPER,z,toggle_scratchpad

# scroller layout
bind=ALT,e,set_proportion,1.0
bind=ALT,x,switch_proportion_preset,

# switch layout
bind=SUPER,n,switch_layout

# tag switch
bind=SUPER,Up,viewtoleft,0
bind=SUPER+ALT,Up,viewtoleft_have_client,0
bind=SUPER,Down,viewtoright,0
bind=SUPER+ALT,Down,viewtoright_have_client,0
bind=CTRL+SUPER,Up,tagtoleft,0
bind=CTRL+SUPER,Down,tagtoright,0

bind=SUPER,1,view,1,0
bind=SUPER,2,view,2,0
bind=SUPER,3,view,3,0
bind=SUPER,4,view,4,0
bind=SUPER,5,view,5,0
bind=SUPER,6,view,6,0
bind=SUPER,7,view,7,0
bind=SUPER,8,view,8,0
bind=SUPER,9,view,9,0

# tag: move client to the tag and focus it
# tagsilent: move client to the tag and not focus it
# bind=Alt,1,tagsilent,1
bind=Alt,1,tag,1,0
bind=Alt,2,tag,2,0
bind=Alt,3,tag,3,0
bind=Alt,4,tag,4,0
bind=Alt,5,tag,5,0
bind=Alt,6,tag,6,0
bind=Alt,7,tag,7,0
bind=Alt,8,tag,8,0
bind=Alt,9,tag,9,0

# monitor switch
bind=alt+shift,Left,focusmon,left
bind=alt+shift,Right,focusmon,right
bind=SUPER+Alt,Left,tagmon,left
bind=SUPER+Alt,Right,tagmon,right

# gaps
bind=ALT+SHIFT,X,incgaps,4
bind=ALT+SHIFT,Z,incgaps,-4
bind=ALT+SHIFT,R,togglegaps

# movewin
bind=CTRL+SHIFT,Up,movewin,+0,-50
bind=CTRL+SHIFT,Down,movewin,+0,+50
bind=CTRL+SHIFT,Left,movewin,-50,+0
bind=CTRL+SHIFT,Right,movewin,+50,+0

# resizewin
#bind=CTRL+ALT,Up,resizewin,+0,-50
#bind=CTRL+ALT,Down,resizewin,+0,+50
#bind=CTRL+ALT,Left,resizewin,-50,+0
#bind=CTRL+ALT,Right,resizewin,+50,+0

# Mouse Button Bindings
# btn_left and btn_right can't bind none mod key
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=SUPER,btn_middle,togglemaximizescreen,0
mousebind=SUPER,btn_right,moveresize,curresize


# Axis Bindings
axisbind=SUPER,UP,viewtoleft_have_client
axisbind=SUPER,DOWN,viewtoright_have_client

# Media
bind=none,XF86AudioMute,spawn,sh -c 'wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle'
bind=none,XF86AudioLowerVolume,spawn,sh -c 'wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-'
bind=none,XF86AudioRaiseVolume,spawn,sh -c 'wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+'
bind=none,XF86AudioMicMute,spawn,sh -c 'wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle'
bind=none,XF86MonBrightnessDown,spawn,sh -c 'brightnessctl s 5%-'
bind=none,XF86MonBrightnessUp,spawn,sh -c 'brightnessctl s 5%+'
